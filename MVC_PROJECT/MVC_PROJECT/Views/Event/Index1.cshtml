@model IEnumerable<MVC_PROJECT.Models.Race>

@{
    ViewBag.Title = "Index1";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>Index1</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RaceDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RaceLocation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RaceDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RaceLocation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RaceNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.RaceNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RaceNumber })
        </td>
    </tr>
}

</table>
